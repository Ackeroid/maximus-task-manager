import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";
import "./index.css";

// This displays the whole React app on the page
ReactDOM.createRoot(document.getElementById("root")).render(<App />);